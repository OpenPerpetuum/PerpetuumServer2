<UserControl x:Class="Perpetuum.AdminTool.Views.TranslationsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#F8F8F8" Padding="8" BorderBrush="#DDD" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <Button Content="Reload" Padding="10,2" Margin="0,0,8,0" Click="OnReloadClick"/>
                <Button Content="Save" Padding="10,2" Margin="0,0,8,0" FontWeight="Bold" Click="OnSaveClick"/>
                <Separator Margin="6,0"/>
                <Button Content="Add key..." Padding="10,2" Margin="0,0,8,0" Click="OnAddKeyClick"/>
                <Button Content="Add language..." Padding="10,2" Margin="0,0,8,0" Click="OnAddLanguageClick"/>
                <Button Content="Remove selected" Padding="10,2" Margin="0,0,8,0" Click="OnRemoveSelectedClick"/>
            </StackPanel>
        </Border>

        <Border DockPanel.Dock="Bottom" Padding="8" BorderBrush="#DDD" BorderThickness="0,1,0,0">
            <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="DimGray"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding StatusIsError}" Value="True">
                                <Setter Property="Foreground" Value="DarkRed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Border>

        <DataGrid x:Name="Grid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  HeadersVisibility="Column"
                  GridLinesVisibility="All"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  SelectedItem="{Binding SelectedRow}"
                  ItemsSource="{Binding Rows}"/>
    </DockPanel>
</UserControl>
