<Window x:Class="Perpetuum.AdminTool.Views.NewEntityWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="New entity"
        Width="460" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="14">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="160"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2"
                   TextWrapping="Wrap" Margin="0,0,0,12"
                   Text="Creates a new entitydefaults row in memory. The definition id is autoincrement and will be assigned by SQL Server when the INSERT runs. Fill in the rest in the detail panel and click Save changes to enqueue."/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="definitionName:" Margin="0,4" VerticalAlignment="Center"/>
        <TextBox  Grid.Row="1" Grid.Column="1" Margin="0,4" x:Name="NameBox"
                  Text="{Binding DefinitionName, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="2" Grid.ColumnSpan="2" Margin="0,8,0,0"
                   Foreground="DarkRed" TextWrapping="Wrap"
                   Text="{Binding ErrorMessage}"/>

        <StackPanel Grid.Row="3" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="Create" Padding="14,4" Margin="0,0,8,0" IsDefault="True" Click="OnCreateClick"/>
            <Button Content="Cancel" Padding="10,4" IsCancel="True" Click="OnCancelClick"/>
        </StackPanel>
    </Grid>
</Window>
